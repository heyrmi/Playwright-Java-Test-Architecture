# target execution: local or remote
target = local

# initial URL
url.base = http://github.com/heyrmi

# global test timeout
timeout = 3

# javafaker locale
faker.locale = pt-BR

# headless mode for all browsers
headless = false

# broser version
browser.version = latest